# list_smith example

A runnable demo of [`list_smith`](../). It's a placeholder for now: the package's list widgets are
still being designed, so the app only wires up the dependency. It will grow into a real
pagination / pull-to-refresh / search showcase once the API lands.

## Running

```sh
cd example
flutter run
```
