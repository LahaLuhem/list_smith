# uv-managed virtualenv + Python caches. `uv.lock` and pyproject.toml stay tracked.
.venv/
__pycache__/
*.py[cod]
.ruff_cache/

# pytest-cov / coverage.py data + reports (regenerated per run, never committed).
.coverage
.coverage.*
htmlcov/
coverage.xml
